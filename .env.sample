# Example environment variables for Guardio
# Copy to `.env` and fill values as needed

# Gemini API key for Google GenAI (optional)
GEMINI_API_KEY=

# When true, AI calls are stubbed and no external requests are made
GUARDIO_DISABLE_AI=false

# Example secret API key for backend auth (used in tests)
GUARDIO_API_KEY=changeme
